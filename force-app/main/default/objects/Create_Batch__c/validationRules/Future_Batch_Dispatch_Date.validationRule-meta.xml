<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Future_Batch_Dispatch_Date</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    OR(
        AND(
            ISPICKVAL(PRIORVALUE(Status__c), &apos;Ready for Dispatch&apos;),
            ISPICKVAL(Status__c, &apos;Dispatched&apos;)
        ),
        AND(
            ISPICKVAL(PRIORVALUE(Status__c), &apos;Dispatched&apos;),
            ISPICKVAL(Status__c, &apos;Ready for Dispatch&apos;)
        )
    ),
    NOT(ISBLANK(Batch_Dispatch_Date__c)),
    DATEVALUE(Batch_Dispatch_Date__c) != TODAY()
)</errorConditionFormula>
    <errorDisplayField>Batch_Dispatch_Date__c</errorDisplayField>
    <errorMessage>Update Batch Dispatch Date to current date.</errorMessage>
</ValidationRule>
